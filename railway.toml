[build]
builder = "dockerfile"
dockerfilePath = "Dockerfile"

[deploy]
# Dockerfile CMD uses start.py which reads PORT env var
healthcheckPath = "/"
healthcheckTimeout = 300
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
